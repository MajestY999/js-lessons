//Повторение циклов

for(let i = 1; i < 10;  i = i + 2){
console.log(`${i}`);
}



const arr = ['!', 'js', 'люблю', 'я']
const newArr = []

for(let i = arr.length -1; i >=0; i--){
    newArr.push(arr[i])
}

console.log(newArr);


for(let i = 1; i <= 15; i += 2){
    console.log(`${i}`);
}

// писать шаг нужно только через равно то есть  i += 2 тогда то есть у нас выводиться будет например только например 1 3 5  7 9 11 и т.д


//Break and Continue

const tasks = ['Задача 1', 'Задача 2', 'Задача 3']


for(let i = 0; i <  tasks.length; i+= 1){// с нуля мы начинаем потому что в нашем массиве все начинается с нклевого индекса.
    if(tasks[i] === 'Задача 2') {// тут мы просто пропускаем Задача 2 но и продолжая цикл до 3 задачи
        continue;// Ну и эта функция нужна для того чтобы специально пропустить какой 
    }
    console.log(tasks[i]);
  
}

console.log('-----------------------------------------------');


for(let i = 0; i <  tasks.length; i+= 1){// с нуля мы начинаем потому что в нашем массиве все начинается с нклевого индекса.
    if(tasks[i] === 'Задача 2') {
       break;//Тут как раз на элементе Задача 2 останавливается весь цикл, то есть выводится только первая задача и работа на этом заканчивается
    }
    console.log(tasks[i]);
  
}

//continue; А continue нужен для того чтобы пропустить  какой либо элемент и продолжить выполнение какого либо кода
//break : когда мы попадаем на break, то работу всего цикла нужно прекратить. И он нужен ятобы дойти до какого элемента, найти его ил и после этого прекратить  выполнение всего цикла


const arrayMy = ["!", "js", "люблю", "я"]
const newarray = []

for(let i = arrayMy.length - 1; i >= 0; i--){  //мы пишем array.lenght - 1 потому что у нас в новом массиве будет 1 элемент пустой а только потом и наши слова то есть если напишем - 0 то будет undefaend
    newarray.push(arrayMy[i])
}

console.log(newarray);


// Пошаговая работа вашего кода
// Начало:
// const arrayMy = ["!", "js", "люблю", "я"] — ваш исходный массив. Его длина (arrayMy.length) равна 4.
// const newarray = [] — ваш пустой массив, в который будут добавляться элементы.
// Запуск цикла: for(let i = arrayMy.length - 1; i >= 0; i--)
// let i = arrayMy.length - 1 → i инициализируется значением 4 - 1, то есть 3.
// i >= 0 — условие продолжения цикла. Пока i больше или равно 0, цикл будет работать.
// i-- — после каждой итерации значение i уменьшается на 1.
// Итерация 1:
// i равно 3.
// newarray.push(arrayMy[3]) — в newarray добавляется элемент arrayMy с индексом 3, то есть строка "я".
// newarray теперь ["я"].
// Итерация 2:
// i уменьшается и становится 2.
// newarray.push(arrayMy[2]) — добавляется элемент с индексом 2, то есть "люблю".
// newarray теперь ["я", "люблю"].
// Итерация 3:
// i уменьшается и становится 1.
// newarray.push(arrayMy[1]) — добавляется элемент с индексом 1, то есть "js".
// newarray теперь ["я", "люблю", "js"].
// Итерация 4:
// i уменьшается и становится 0.
// newarray.push(arrayMy[0]) — добавляется элемент с индексом 0, то есть "!".
// newarray теперь ["я", "люблю", "js", "!"].
// Завершение цикла:
// i уменьшается и становится -1.
// Условие i >= 0 становится ложным, и цикл прекращается.
// Исправление ваших заблуждений